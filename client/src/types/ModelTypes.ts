export type ImageModelTypes = {
  id: number;
  name: string;
  modelType: string;
  backgroundImage: string;
  provider: string;
};
export type TextModelTypes = {
  id: number;
  modelType: string;
  endPoint: string;
  name: string;
};
